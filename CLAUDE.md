
<!-- claude-learn pack: typescript -->
## Project conventions (auto-added by claude-learn)

- TypeScript project — strict types preferred; avoid `any`.
- **Do not read `node_modules/` or `dist/` or `.next/` or `build/` ever**.
- When editing existing code, prefer `Edit` over `Write` for small changes.
- For imports, use relative paths unless a `paths` alias or `#imports` field is configured.
- Run the typechecker (`tsc --noEmit`) or linter before assuming code is correct.


<!-- claude-learn pack: nodejs -->
## Node.js project conventions (auto-added by claude-learn)

- **Do not read `node_modules/` ever** — tens of thousands of files, zero value.
- Check `package.json` scripts before suggesting commands; use those over raw `node`/`tsc` when available.
- Prefer `pnpm` if `pnpm-lock.yaml` exists, else `npm`, else `yarn`.


## Response style (auto-added by claude-learn)
- Be concise. No preamble. No summary at the end of every response.
- Use short sentences and bullet lists over paragraphs.
- Show code, not narration about the code.
- If nothing to add beyond the answer, stop writing.

## Project primer (auto-generated by claude-learn)
<!-- Refreshes when project structure changes. Helps Claude resume after context overflow without re-exploration. -->

### Structure
- `CLAUDE.md` (file)
- `LICENSE` (file)
- `README.md` (file)
- `bin` (dir)
- `cat` (file)
- `config` (dir)
- `deploy` (dir)
- `dist` (dir)
- `macbeth coursework.docx` (file)
- `menubar` (dir)
- `node_modules` (dir)
- `package-lock.json` (file)
- `package.json` (file)
- `plan.md` (file)
- `rust-sidecar` (dir)

### Stack markers
- **Node/TS** (found `package.json`)

### Conventions detected
- (populated from claude-learn memory over time)

## Key file: src/modules/flash-protocol.ts
<!-- Add a one-paragraph summary of what this file exports/contains. -->

## Key file: src/index.ts
- **depends on:** `readline`, `child_process`, `fs`
- **exports (fn):** `boot`


## Key file: watch/JarvisWatch/JarvisWatch.xcodeproj/project.pbxproj
<!-- Add a one-paragraph summary of what this file exports/contains. -->

## Key file: watch/JarvisWatch/JarvisWatch/WatchConnectionManager.swift
<!-- Add a one-paragraph summary of what this file exports/contains. -->

## Key file: src/utils/voice-output.ts
- **depends on:** `child_process`, `fs`, `path`, `os`
- **exports (fn):** `isVoiceEnabled`, `enableVoice`, `disableVoice`, `toggleVoice`, `setVoice`, `getVoice`, `setRate`, `getRate`, `isSpeaking`, `stopSpeaking`, `abortSpeech`, `resetSpeechAbort`


## Key file: Users/arhanharchandani/Downloads/JarvisWeb/src/App.jsx
- **depends on:** `react`


## Key file: src/watch/aim-bridge.ts
- **depends on:** `ws`, `fs`, `path`, `os`, `child_process`, `util`
- **exports (fn):** `startAIMBridge`, `stopAIMBridge`, `isAIMConnected`, `broadcastStatusViaAIM`


## Key file: watch/JarvisWatch/JarvisWatch/ContentView.swift
<!-- Add a one-paragraph summary of what this file exports/contains. -->

## Key file: tmp/mark-xxxv/main.py
<!-- Add a one-paragraph summary of what this file exports/contains. -->

## Key file: watch/JarvisWatch/JarvisCompanion/ContentView.swift
<!-- Add a one-paragraph summary of what this file exports/contains. -->

## Key file: watch/JarvisWatch/JarvisCompanion/iOSConnectionManager.swift
<!-- Add a one-paragraph summary of what this file exports/contains. -->

## Inline summary: README.md
- **sections:** _J.A.R.V.I.S._, _Docs_, _Quick Start_, _Features_, _Voice Assistant_, _Conversation Engine_, _All Commands_, _Multi-Device Support_, _Command Chaining_, _Variables_


## Inline summary: src/modules/flash-protocol.ts
<!-- Add a summary here to prevent re-reads. -->

## Inline summary: src/index.ts
- **depends on:** `readline`, `child_process`, `fs`
- **exports (fn):** `boot`


## Inline summary: watch/JarvisWatch/JarvisWatch.xcodeproj/project.pbxproj
<!-- Add a summary here to prevent re-reads. -->

## Key file: src/modules/comms-stack.ts
- **classes:** `CommsStackModule`


## Key file: src/utils/network-guardian.ts
- **depends on:** `child_process`, `util`
- **exports (fn):** `startNetworkGuardian`, `stopNetworkGuardian`, `trustDevice`, `getNetworkDevices`, `runManualScan`


## Key file: src/modules/screen-watcher.ts
- **depends on:** `child_process`, `util`
- **classes:** `ScreenWatcher`


## Key file: src/core/conversation-engine.ts
- **types:** `ConversationOptions`, `ConversationResponse`
- **const/let:** `conversationEngine`


## Key file: src/mac-client.ts
- **depends on:** `readline`, `ws`, `child_process`, `util`, `fs`, `path`, `url`, `os`


## Key file: watch/JarvisWatch/JarvisWatch/SpeechRecognizer.swift
<!-- Add a one-paragraph summary of what this file exports/contains. -->

## Inline summary: watch/JarvisWatch/JarvisWatch/WatchConnectionManager.swift
<!-- Add a summary here to prevent re-reads. -->

## Inline summary: src/utils/voice-output.ts
- **depends on:** `child_process`, `fs`, `path`, `os`
- **exports (fn):** `isVoiceEnabled`, `enableVoice`, `disableVoice`, `toggleVoice`, `setVoice`, `getVoice`, `setRate`, `getRate`, `isSpeaking`, `stopSpeaking`, `abortSpeech`, `resetSpeechAbort`


## Inline summary: Users/arhanharchandani/Downloads/JarvisWeb/src/App.jsx
- **depends on:** `react`


## Key file: src/core/parser.ts
- **exports (fn):** `invalidateAliasCache`, `levenshtein`, `splitChainedCommands`, `parse`


## Inline summary: watch/JarvisWatch/JarvisWatch/ContentView.swift
<!-- Add a summary here to prevent re-reads. -->

## Key file: src/utils/llm.ts
- **exports (fn):** `getLastUsedLabel`, `llmStreamChat`, `isLLMAvailable`, `getActiveLLMProvider`, `getLLMConfig`, `setClaudeApiKey`, `setLLMProvider`


## Inline summary: tmp/mark-xxxv/main.py
<!-- Add a summary here to prevent re-reads. -->

## Key file: src/modules/system-control.ts
- **classes:** `SystemControlModule`


## Key file: src/core/types.ts
- **types:** `type ModuleName`, `ParsedCommand`, `CommandResult`, `PatternDefinition`, `JarvisModule`


## Tool-use tip (auto-added by claude-learn)
- You often run `Write → Read → Read` in sequence. When possible, combine these into a single bash pipe or use one broader tool call.

## Key file: src/utils/rust-bridge.ts
- **depends on:** `child_process`, `fs`, `path`, `url`
- **exports (fn):** `isSidecarAvailable`, `startSidecar`, `stopSidecar`, `indexDocument`, `bulkIndex`, `vectorSearch`, `embedText`, `deleteDocument`, `getSidecarStats`, `fuzzyMatch`, `levenshtein`, `batchLevenshtein`
- **types:** `VectorSearchResult`, `FuzzyMatchResult`, `KeywordEntry`, `TraceAnalytics`, `HabitPattern`


## Frequent search: `ollama|Ollama`
<!-- Describe where this symbol/text lives so Claude doesn't have to search. -->

## Key file: src/core/memory.ts
- **depends on:** `fs`
- **exports (fn):** `loadMemory`, `addFact`, `getFact`, `searchFacts`, `getAllFacts`, `removeFact`, `addConversationEntry`, `getRecentConversation`, `getAllConversations`, `clearConversation`, `setConversations`, `addSummary`
- **types:** `MemoryFact`, `ConversationEntry`, `ConversationSummary`


## Inline summary: src/core/conversation-engine.ts
- **types:** `ConversationOptions`, `ConversationResponse`
- **const/let:** `conversationEngine`


## Common command
`node -e <PATH> 2>&1` — used routinely in this project.

## Common command
`cd <PATH> && python3 -c <PATH>` — used routinely in this project.

## Key file: src/modules/ai-chat.ts
- **depends on:** `fs`, `path`, `os`
- **exports (fn):** `getLastStreamedText`, `getActiveModel`
- **classes:** `AIChatModule`


## Key file: src/modules/whatsapp.ts
- **depends on:** `child_process`
- **classes:** `WhatsAppModule`


## Inline summary: src/utils/llm.ts
- **depends on:** `undici`
- **exports (fn):** `getLastUsedLabel`, `llmStreamChat`, `llmQuick`, `isLLMAvailable`, `getActiveLLMProvider`, `getLLMConfig`, `setClaudeApiKey`, `setLLMProvider`
- **types:** `LLMOptions`
- **const/let:** `DEFAULT_MODEL`, `FAST_MODEL`


## Key file: src/modules/deep-research.ts
- **depends on:** `child_process`, `fs`, `path`, `url`


## Key file: .arhan.dev/src/Docs.jsx
- **depends on:** `react`
- **classes:** `MyModule`


## Key file: .arhan.dev/src/App.jsx
- **depends on:** `react`
- **exports (fn):** `App`


## Key file: src/voice/voice-assistant.ts
- **depends on:** `child_process`, `readline`, `fs`, `path`, `url`
- **classes:** `VoiceAssistant`


## Inline summary: src/voice/voice-assistant.ts
- **depends on:** `child_process`, `readline`, `fs`, `path`, `url`
- **classes:** `VoiceAssistant`


## Key file: src/utils/status-reporter.ts
- **depends on:** `fs`
- **exports (fn):** `onStatusUpdate`, `setAIMStatusBroadcast`, `reportBoot`, `reportVoice`, `reportState`, `reportCommand`, `reportShutdown`


## Key file: src/utils/whatsapp-baileys.ts
- **depends on:** `fs`, `pino`
- **exports (fn):** `startWhatsApp`, `sendWhatsApp`, `recentInbound`, `waStatus`, `isWhatsAppConnected`, `repairWhatsApp`
- **types:** `type ConnectionState`, `InboundMessage`, `StartOpts`


## Key file: menubar/JarvisMenubar.swift
<!-- Add a one-paragraph summary of what this file exports/contains. -->

## Inline summary: menubar/JarvisMenubar.swift
<!-- Add a summary here to prevent re-reads. -->

## Key file: src/modules/screen-awareness.ts
- **depends on:** `os`, `path`, `fs`
- **exports (fn):** `captureScreenText`, `captureScreenImage`
- **classes:** `ScreenAwarenessModule`


## Inline summary: src/watch/aim-bridge.ts
- **depends on:** `ws`, `fs`, `path`, `os`, `child_process`, `util`
- **exports (fn):** `startAIMBridge`, `stopAIMBridge`, `isAIMConnected`, `broadcastStatusViaAIM`


## Key file: src/modules/app-launcher.ts
- **depends on:** `child_process`
- **classes:** `AppLauncherModule`


## Key file: src/modules/timer.ts
- **classes:** `TimerModule`


## Key file: src/modules/research.ts
- **depends on:** `child_process`, `fs`, `path`, `url`
- **classes:** `ResearchModule`


## Key file: src/watch/ws-server.ts
- **depends on:** `ws`, `http`, `fs`, `path`, `os`, `child_process`, `util`
- **exports (fn):** `startWatchServer`, `stopWatchServer`, `getWatchClientCount`


## Inline summary: src/watch/ws-server.ts
- **depends on:** `ws`, `http`, `fs`, `path`, `os`, `child_process`, `util`
- **exports (fn):** `startWatchServer`, `stopWatchServer`, `getWatchClientCount`


## Inline summary: .arhan.dev/src/Docs.jsx
- **depends on:** `react`
- **classes:** `MyModule`


## Inline summary: src/core/parser.ts
- **exports (fn):** `invalidateAliasCache`, `learnRoute`, `levenshtein`, `splitChainedCommands`, `parse`

